RSpec.describe Tanita::Api::Client do
  it "has a version number" do
    expect(Tanita::Api::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
